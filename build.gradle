plugins {
    id 'java'
}

group 'ru.nikiens'
version '1.0-SNAPSHOT'

repositories {
    mavenCentral()
}

dependencies {
    testCompile 'junit:junit:4.12'
    testCompile group: 'com.google.guava', name: 'guava-testlib', version: '28.0-jre'}

test {
    useJUnit()
}
